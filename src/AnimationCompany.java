public class AnimationCompany extends Company{
    public AnimationCompany(String name, int revenue, int expense, int budget, Worker[] workers) {
        super(name, revenue, expense, budget, workers);
    }
}
